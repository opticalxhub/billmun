import { ArrowRight, Check, X, Menu, User, Settings, LogOut, ChevronDown, Mail } from "lucide-react"

export const Icons = {
  arrowRight: ArrowRight,
  check: Check,
  close: X,
  menu: Menu,
  user: User,
  settings: Settings,
  logout: LogOut,
  chevronDown: ChevronDown,
  mail: Mail,
}
